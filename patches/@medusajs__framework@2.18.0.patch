diff --git a/dist/http/router.js b/dist/http/router.js
index ea8f44de0e4c0b70ac4720f82a69418269c5270e..c0883b8a66c4189c73b8761fb1cd8abe41521345 100644
--- a/dist/http/router.js
+++ b/dist/http/router.js
@@ -24,6 +24,7 @@ const middleware_file_loader_1 = require("./middleware-file-loader");
 const middlewares_1 = require("./middlewares");
 const bodyparser_1 = require("./middlewares/bodyparser");
 const check_permissions_1 = require("./middlewares/check-permissions");
+const request_permissions_1 = require("../policies/has-permission");
 const ensure_publishable_api_key_1 = require("./middlewares/ensure-publishable-api-key");
 const error_handler_1 = require("./middlewares/error-handler");
 const routes_finder_1 = require("./routes-finder");
@@ -51,6 +52,12 @@ class ApiLoader {
         __classPrivateFieldSet(this, _ApiLoader_logger, container.resolve(utils_1.ContainerRegistrationKeys.LOGGER), "f");
     }
     async load() {
+        __classPrivateFieldGet(this, _ApiLoader_app, "f").use((req, _res, next) => {
+            if (req.method === "GET" || req.method === "HEAD") {
+                (0, request_permissions_1.initializeReadOnlyRequestPolicyCache)(req.scope);
+            }
+            next();
+        });
         if (utils_1.FeatureFlag.isFeatureEnabled("backend_hmr")) {
             ;
             global.__MEDUSA_HMR_API_LOADER__ = this;
diff --git a/dist/policies/has-permission.js b/dist/policies/has-permission.js
index 9b5b8a9d9f6abf1a5185c3024e21165517aa9b4a..b0fd09e7a2271c38860ef4b44d4a410c2bec83eb 100644
--- a/dist/policies/has-permission.js
+++ b/dist/policies/has-permission.js
@@ -2,7 +2,14 @@
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.hasPermission = hasPermission;
 exports.resolvePermissions = resolvePermissions;
+exports.initializeReadOnlyRequestPolicyCache = initializeReadOnlyRequestPolicyCache;
 const utils_1 = require("@medusajs/utils");
+// Only the HTTP router enrolls GET/HEAD scopes. Root containers, jobs and
+// mutation workflows must always observe fresh permissions.
+const requestPolicyCaches = new WeakMap();
+function initializeReadOnlyRequestPolicyCache(container) {
+    requestPolicyCaches.set(container, new Map());
+}
 /**
  * Applies wildcard-aware matching to decide whether any of the
  * supplied roles grants `(resource, operation)`. This is the single source of
@@ -108,36 +115,37 @@ async function resolvePermissions(input) {
  * Fetches a single role's policies from cache or database.
  */
 async function fetchSingleRolePolicies(roleId, container) {
+    const cache = requestPolicyCaches.get(container);
+    if (!cache) {
+        return readSingleRolePolicies(roleId, container);
+    }
+    if (!cache.has(roleId)) {
+        const pending = readSingleRolePolicies(roleId, container);
+        cache.set(roleId, pending);
+        pending.catch(() => {
+            if (cache.get(roleId) === pending) cache.delete(roleId);
+        });
+    }
+    return cache.get(roleId);
+}
+async function readSingleRolePolicies(roleId, container) {
     const query = container.resolve(utils_1.ContainerRegistrationKeys.QUERY);
-    const tags = [];
-    return await (0, utils_1.useCache)(async () => {
         const { data: roles } = await query.graph({
             entity: "rbac_role",
             fields: ["id", "policies.*"],
             filters: { id: roleId },
-        });
+        }, { cache: { enable: false } });
         const role = roles[0];
         const resourceMap = new Map();
-        tags.push(`rbac_role:${roleId}`);
         if (role?.policies && Array.isArray(role.policies)) {
-            const policyIds = [];
             for (const policy of role.policies) {
-                policyIds.push(policy.id);
                 if (!resourceMap.has(policy.resource)) {
                     resourceMap.set(policy.resource, new Set());
                 }
                 resourceMap.get(policy.resource).add(policy.operation);
-                tags.push(`rbac_policy:${policy.id}`);
             }
         }
         return resourceMap;
-    }, {
-        container,
-        key: roleId,
-        tags,
-        ttl: 60 * 60 * 24 * 7,
-        providers: ["cache-memory"],
-    });
 }
 /**
  * Fetches policies for multiple roles by composing individually cached role queries.
